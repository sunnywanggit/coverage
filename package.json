{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },

  "dependencies": {
    "lottie-web": "^5.10.1"
  },
  "devDependencies": {
    "nuxt": "3.0.0",
    "vite-plugin-istanbul": "^4.0.0-rc.1"
  }
}
